<main>
  <h1><%= @message %></h1>

  <% if @status == :valid %>
    <a href="/namespaces" class="btn btn-primary">dashboard</a>
  <% else %>
    <a href="/" class="btn btn-primary">homepage</a>
  <% end %>

</main>

<style>
  main {
    width: 100vw;
    height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-direction: column;
    gap: 1rem;
  }

  main > img {
    width: 20rem;
  }
</style>
